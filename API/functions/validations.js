// --------------------------------------------------------
// --------------- VALIDATIONS AND MIDDLEWARES MODULE -------------------
// --------------------------------------------------------
// ---*** IMPORTACIONES ***--- (if so)

//todo esto tiene q ir dentro del repos en una func async!
/*
const searchIt = Author.findOne({ where: { idA: id } });
If (searchIt):
	console.log('error! Este x con este xPK ya existe!');
else:
	const newAuthor = await Author.create(author);
    	return newAuthor; */

